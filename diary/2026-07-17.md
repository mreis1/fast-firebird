# 2026-07-17 — Session 40: npm publish machinery

npm org `fast-firebird` registered by Marcio → built everything between here
and the first `v0.1.0` release:

- **Per-package READMEs** (`packages/core/README.md`, `packages/drizzle/…`) —
  npm renders the package page from the *tarball's* README, so without these
  the pages would be blank. Core: positioning + highlights + quick start;
  drizzle: what-works list (incl. the honest `with:` boundary). Drizzle
  example verified against actual `index.ts` exports (`firebirdTable`,
  `integer`, `varchar`, `drizzle` — all real).
- **`publishConfig: { access: "public" }`** in both packages (scoped packages
  default to private publish, which fails without a paid plan). Provenance
  deliberately NOT in publishConfig — it comes from the CI env var, so a
  local emergency publish stays possible.
- **`.github/workflows/publish.yml`** — triggers on GitHub Release published;
  same pnpm setup + references-stub as ci.yml; build + core unit tests as a
  final gate (the full Firebird matrix is ci.yml's job on every push);
  `pnpm -r publish --access public --no-git-checks` with
  `NPM_CONFIG_PROVENANCE=true` + `id-token: write`. `-r` skips the private
  packages and **rewrites `workspace:*` to the real version** — which is why
  publishing must go through pnpm, never raw `npm publish` (drizzle's
  manifest would ship a broken `workspace:*` dependency).
- **Tarballs verified** (`npm pack --dry-run`): core = LICENSE, NOTICE,
  README, dist, package.json (567 kB, 10 files); drizzle = LICENSE, README,
  dist (47 kB, 9 files). No stray files.

Remaining, all user-side: decide on private-repo mentions in plans/diary →
flip repo public (npm provenance REQUIRES a public repo — Sigstore
transparency log), create an npm Granular Access Token (write on the
fast-firebird org) → repo secret `NPM_TOKEN`, enable Discussions, then create
GitHub Release `v0.1.0` — the workflow does the rest.

## Addendum — history-rewrite rehearsal + README positioning

Marcio decided the compat-layer mentions are private (they reference a
personal/company repo). Working-tree text was masked in d84e0d3; the strings
survive in older commits, so a `git filter-repo --replace-text` rewrite was
REHEARSED on a fresh `--no-local` clone in the scratchpad:

- Full-history scan first: company strings in 3 files, absolute `~/`-paths in
  3 research files, commit messages clean, no emails/IPs/credentials in any
  historical blob ("secret" hits are example passwords + SRP terminology).
- Verification after rewrite: 0 matches across all revs for both patterns;
  HEAD tree hash byte-identical (8412bede…) — the rewrite only touches
  historical blobs; 58 commits in/out. Compat-layer code itself was confirmed
  safe in its own private repo (branch pushed to its origin) — this repo only
  ever carried doc mentions.
- Runbook for the real run (on Marcio's go): fresh `--no-local` clone,
  `git filter-repo --replace-text replacements.txt`,
  re-add origin (filter-repo strips remotes on purpose), `push --force
  --all`+`--tags`. Replacement-rules files live OUTSIDE the repo
  (committing them would reintroduce the strings).

README "Why another Firebird driver?": folded in Marcio's framing — respect
for node-firebird (carried his production systems for years), its limits
attributed to decade-scale technical debt + deliberate retro-compat with very
old Node versions; fast-firebird stated as the deliberately disruptive
opposite (no baggage, Node ≥ 20 baseline, modern practices).

## Addendum 2 — new phoenix branding

Marcio supplied a new AI-generated phoenix mark (`logo1.png`, root). Its
"transparency" was a baked-in checkerboard → removed via chroma key (the mark
is pure orange/red, the checker pure neutral 232–255 gray): alpha =
smoothstep(chroma 10→130), then un-blend edge pixels against bg≈246. Verified
halo-free on dark + white.

New kit in `assets/` (old kit kept untouched): `logo1-transparent`,
`logo1-lettering` (mark + wordmark), `logo2-lettering` (+ tagline "The modern,
pure-TypeScript Firebird driver for Node.js"), each as PNG and SVG. SVGs embed
the mark as base64 (gradient raster, no vectorizer available — tracing would
posterize) with real `<text>` wordmark/tagline, `textLength`-locked to
PIL-measured widths; rendering verified in headless Chrome. Wordmark style
matches the old logo.svg (`#C0562A` / `#FF6A2C→#FFB13C` / mono `#8b99a8`).

Applied: root README banner → `logo2-lettering.svg`; core+drizzle READMEs got
a raw.githubusercontent banner (`png/logo2-lettering-1440.png` — renders on
npm once the repo is public; raw SVG won't, wrong content-type); demo
`public/` favicon set regenerated from the phoenix (mark.svg 256-embed,
favicon.ico 16/32/48, mark-180.png, logo.svg = logo2) + copied into dist-web.
